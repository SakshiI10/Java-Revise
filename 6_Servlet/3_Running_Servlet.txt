1. Create a class by right clicking on project->new->other->class->Enter package and Name of the class->Enter.

2. Create an index.html file inside the webapp. Add a form that submits data to the servlet:
Eg:
<form action="message" method="Post">
	Enter your name: <input type="text" name="name">
	<input type="submit">
</form> 
NOTE: IT'S RETURNS A PAGE

3. Map the Servlet to the URL in web.xml (if not using Annotations). Inside WEB-INF/web.xml, add:
<servlet>
        <servlet-name>message</servlet-name>
        <servlet-class>com.display.message</servlet-class>
</servlet>
<servlet-mapping>
        <servlet-name>message</servlet-name>
        <url-pattern>/message</url-pattern>
</servlet-mapping>
NOTE: IT'S RETURNS USER DATA NOT A PAGE


4. Map the servlet to the URL in the class itself by using Annotations
@WebServlet("/annotationsDemo")
NOTE: IT'S RETURNS USER DATA NOT A PAGE
